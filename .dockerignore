# Every line below either bloats the image or drags into it something that
# does not belong there.

.venv/
venv/
__pycache__/
*.py[cod]

# The development database holds demo data and the demo user's token.
# Inside an image layer they would stay forever.
db.sqlite3
*.sqlite3
.env

.git/
.github/
.pytest_cache/
.ruff_cache/
*.md
!README.md
